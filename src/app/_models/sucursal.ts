import { Response } from './response';

export class Sucursal {
    codigo?: number;
    descripcion?: string;
    direccion?: string;
    identificacion?: string;
    fechaCreacion?: Date;
    codigoMoneda?: string;
}